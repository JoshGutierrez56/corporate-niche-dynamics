# P19E — Final Repository Closeout

## Purpose

P19E closes the Business Niche Hypercube research program after the validated
P18E boundary. It is a reporting and archival phase only. It may summarize the
original P0-P10 pipeline and the signed P13F-P18E redesign sequence, but it may
not fit another model, search another signal, tune clustering, run real data,
or promote a non-primary result.

The recommended public repository name is `corporate-niche-dynamics`. The
paper-style subtitle remains *Corporate Niche Dynamics and Asset Prices*.

## Frozen evidence

P19E may read only:

- the frozen P0-P10 receipts and report statistics;
- the P13F-P18E manifests, validation reports, and compact result tables;
- the existing P10 README and report for byte-identical archival; and
- the test receipt produced during this closeout.

P19E does not read synthetic truth tables, raw or processed real data, or any
new outcome surface. It does not run P10 again.

## Frozen verdicts

The repository-facing claim ledger must distinguish:

1. `PASS` — engineering reproducibility and point-in-time safeguards;
2. `PASS_SYNTHETIC_6X_ONLY` — the locked anchored-axis innovation detects the
   deliberately amplified synthetic migration-return channel;
3. `REJECT_PRIMARY_IMPLEMENTATION` — the primary conservative portfolio is
   negative after costs;
4. `REJECT_CURRENT_TAXONOMY` — the frozen clustering design is mostly noise
   and unstable;
5. `NOT_TESTED` — real-market existence, causality, and investability; and
6. `COMPLETE_MIXED_NEGATIVE` — the project is complete as a synthetic proof of
   concept and negative implementation/taxonomy result.

The original P10 conclusion remains historically valid for the original
`migration_surprise` specification. The later redesign is exploratory and
synthetic; it does not retroactively turn P10 into a positive confirmatory
result.

## Acceptance gates

1. The original P10 README and report are archived byte-identically against
   their P10 manifest hashes before the repository-facing files change.
2. Every P13F-P18E manifest and every P19E source metric hash recomputes.
3. All reported P16E, P17E, and P18E values exactly match saved compact tables.
4. The final README, report, and claim ledger use the frozen verdicts above.
5. No existing phase manifest or analytical output is modified.
6. The full test suite passes.
7. The concurrently running EDGAR embedding worker is neither stopped,
   duplicated, nor reconfigured.

P19E ends the Hypercube program. Further signal, cost, clustering, or real-data
work requires a new repository or a separately versioned research protocol.
