# P19E Final Repository Closeout Results

## Decision

**PASS — project complete with a mixed/negative research verdict.**

Recommended repository name: **`corporate-niche-dynamics`**

The name describes the durable research subject without implying that the
project discovered investable alpha or a stable economic taxonomy. The local
directory and Python distribution retain the historical
`business-niche-hypercube` name so signed receipts remain reproducible.

## Defensible conclusions

- The original preregistered `migration_surprise` specification is rejected.
- The redesigned `anchored_axis_innovation` measure is a synthetic proof of
  concept at the frozen 6x migration setting: return IC `0.0196809`,
  clustered p-value `0.0021131`; the null control is not significant
  (p-value `0.2541`).
- The frozen primary implementation is rejected after costs: annualized net
  return `-5.1610%`, net Sharpe `-0.8434`.
- The current archetype layer is rejected: `94.676%`–`96.499%` of observations
  are noise/unassigned and cluster stability is effectively zero.
- Real-data validity, causality, and investability were not tested.

## Closeout controls

- Original P10 README and report archived byte-for-byte.
- No analytical model was fit and no synthetic truth or real data was read.
- No P10 phase was rerun.
- All 83 tests passed.
- Independent validation checked 89 P13F–P18E manifest records with zero
  errors.
- The separate EDGAR embedding worker remained alive and unchanged throughout
  closeout.

The project should be archived as a completed synthetic research program. Any
future real-data work should begin under a new preregistered protocol rather
than extending the rejected specifications.
